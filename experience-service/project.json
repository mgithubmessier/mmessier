{
  "name": "experience-service",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "experience-service/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "experience-service/jest.config.ts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "experience-service",
        "command": "bun build --entrypoints ./src/index.ts --outdir ../dist/experience-service --target node --minify && cp package.json ../dist/experience-service/package.json"
      }
    }
  },
  "tags": []
}
