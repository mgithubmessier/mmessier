import { Style } from '../../../types';

export const styles: Style = () => ({
  static: {
    container: {
      display: 'flex',
      flexDirection: 'column',
      width: '100%',
      flex: 1,
    },
  },
});
