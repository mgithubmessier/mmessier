import { BasicStyle } from '../../types';
import { colors } from '../styles/colors';

export const basicStyles: BasicStyle = {
  static: {
    container: {
      width: '100%',
      height: '100%',
    },
  },
};
