{
  "name": "authentication-service",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "authentication-service/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "authentication-service/jest.config.ts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "authentication-service",
        "command": "bun build --entrypoints ./src/index.ts --outdir ../dist/authentication-service --target node --minify && cp package.json ../dist/authentication-service/package.json"
      }
    }
  },
  "tags": []
}
