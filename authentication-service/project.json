{
  "name": "authentication-service",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "authentication-service/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "authentication-service/jest.config.ts"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/authentication-service",
        "main": "authentication-service/src/index.ts",
        "tsConfig": "authentication-service/tsconfig.lib.json"
      }
    },
    "dist-install": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./dist/authentication-service",
        "command": "yarn"
      }
    }
  },
  "tags": []
}
